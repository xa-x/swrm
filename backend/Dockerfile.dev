# Development Dockerfile with Docker socket access
FROM oven/bun:1-alpine

WORKDIR /app

# Install dependencies
COPY package.json bun.lock* ./
RUN bun install

# Copy source
COPY . .

# Expose port
EXPOSE 3001

# Run in dev mode
CMD ["bun", "run", "--watch", "src/index.ts"]
